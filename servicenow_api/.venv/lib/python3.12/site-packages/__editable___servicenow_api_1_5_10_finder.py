from __future__ import annotations
import sys
from importlib.machinery import ModuleSpec, PathFinder
from importlib.machinery import all_suffixes as module_suffixes
from importlib.util import spec_from_file_location
from itertools import chain
from pathlib import Path

MAPPING: dict[str, str] = {'scripts': '/workspaces/snow-api/scripts', 'servicenow_api': '/workspaces/snow-api/servicenow_api', 'tests': '/workspaces/snow-api/tests'}
NAMESPACES: dict[str, list[str]] = {'tests': ['/workspaces/snow-api/tests'], 'scripts': ['/workspaces/snow-api/scripts'], 'servicenow_api.skills': ['/workspaces/snow-api/servicenow_api/skills'], 'servicenow_api.skills.servicenow-plugins': ['/workspaces/snow-api/servicenow_api/skills/servicenow-plugins'], 'servicenow_api.skills.servicenow-activity-subscriptions': ['/workspaces/snow-api/servicenow_api/skills/servicenow-activity-subscriptions'], 'servicenow_api.skills.servicenow-metricbase': ['/workspaces/snow-api/servicenow_api/skills/servicenow-metricbase'], 'servicenow_api.skills.servicenow-auth': ['/workspaces/snow-api/servicenow_api/skills/servicenow-auth'], 'servicenow_api.skills.servicenow-cicd': ['/workspaces/snow-api/servicenow_api/skills/servicenow-cicd'], 'servicenow_api.skills.servicenow-testing': ['/workspaces/snow-api/servicenow_api/skills/servicenow-testing'], 'servicenow_api.skills.servicenow-account': ['/workspaces/snow-api/servicenow_api/skills/servicenow-account'], 'servicenow_api.skills.servicenow-ppm': ['/workspaces/snow-api/servicenow_api/skills/servicenow-ppm'], 'servicenow_api.skills.servicenow-application': ['/workspaces/snow-api/servicenow_api/skills/servicenow-application'], 'servicenow_api.skills.servicenow-product-inventory': ['/workspaces/snow-api/servicenow_api/skills/servicenow-product-inventory'], 'servicenow_api.skills.servicenow-knowledge-management': ['/workspaces/snow-api/servicenow_api/skills/servicenow-knowledge-management'], 'servicenow_api.skills.servicenow-batch': ['/workspaces/snow-api/servicenow_api/skills/servicenow-batch'], 'servicenow_api.skills.servicenow-incidents': ['/workspaces/snow-api/servicenow_api/skills/servicenow-incidents'], 'servicenow_api.skills.servicenow-email': ['/workspaces/snow-api/servicenow_api/skills/servicenow-email'], 'servicenow_api.skills.servicenow-aggregate': ['/workspaces/snow-api/servicenow_api/skills/servicenow-aggregate'], 'servicenow_api.skills.servicenow-table-api': ['/workspaces/snow-api/servicenow_api/skills/servicenow-table-api'], 'servicenow_api.skills.servicenow-cilifecycle': ['/workspaces/snow-api/servicenow_api/skills/servicenow-cilifecycle'], 'servicenow_api.skills.servicenow-hr': ['/workspaces/snow-api/servicenow_api/skills/servicenow-hr'], 'servicenow_api.skills.servicenow-cmdb': ['/workspaces/snow-api/servicenow_api/skills/servicenow-cmdb'], 'servicenow_api.skills.servicenow-custom-api': ['/workspaces/snow-api/servicenow_api/skills/servicenow-custom-api'], 'servicenow_api.skills.servicenow-data-classification': ['/workspaces/snow-api/servicenow_api/skills/servicenow-data-classification'], 'servicenow_api.skills.servicenow-change-management': ['/workspaces/snow-api/servicenow_api/skills/servicenow-change-management'], 'servicenow_api.skills.servicenow-update-sets': ['/workspaces/snow-api/servicenow_api/skills/servicenow-update-sets'], 'servicenow_api.skills.servicenow-service-qualification': ['/workspaces/snow-api/servicenow_api/skills/servicenow-service-qualification'], 'servicenow_api.skills.servicenow-import-sets': ['/workspaces/snow-api/servicenow_api/skills/servicenow-import-sets'], 'servicenow_api.skills.servicenow-devops': ['/workspaces/snow-api/servicenow_api/skills/servicenow-devops'], 'servicenow_api.skills.servicenow-source-control': ['/workspaces/snow-api/servicenow_api/skills/servicenow-source-control'], 'servicenow_api.skills.servicenow-attachment': ['/workspaces/snow-api/servicenow_api/skills/servicenow-attachment']}
PATH_PLACEHOLDER = '__editable__.servicenow_api-1.5.10.finder' + ".__path_hook__"


class _EditableFinder:  # MetaPathFinder
    @classmethod
    def find_spec(cls, fullname: str, path=None, target=None) -> ModuleSpec | None:  # type: ignore
        # Top-level packages and modules (we know these exist in the FS)
        if fullname in MAPPING:
            pkg_path = MAPPING[fullname]
            return cls._find_spec(fullname, Path(pkg_path))

        # Handle immediate children modules (required for namespaces to work)
        # To avoid problems with case sensitivity in the file system we delegate
        # to the importlib.machinery implementation.
        parent, _, child = fullname.rpartition(".")
        if parent and parent in MAPPING:
            return PathFinder.find_spec(fullname, path=[MAPPING[parent]])

        # Other levels of nesting should be handled automatically by importlib
        # using the parent path.
        return None

    @classmethod
    def _find_spec(cls, fullname: str, candidate_path: Path) -> ModuleSpec | None:
        init = candidate_path / "__init__.py"
        candidates = (candidate_path.with_suffix(x) for x in module_suffixes())
        for candidate in chain([init], candidates):
            if candidate.exists():
                return spec_from_file_location(fullname, candidate)
        return None


class _EditableNamespaceFinder:  # PathEntryFinder
    @classmethod
    def _path_hook(cls, path) -> type[_EditableNamespaceFinder]:
        if path == PATH_PLACEHOLDER:
            return cls
        raise ImportError

    @classmethod
    def _paths(cls, fullname: str) -> list[str]:
        paths = NAMESPACES[fullname]
        if not paths and fullname in MAPPING:
            paths = [MAPPING[fullname]]
        # Always add placeholder, for 2 reasons:
        # 1. __path__ cannot be empty for the spec to be considered namespace.
        # 2. In the case of nested namespaces, we need to force
        #    import machinery to query _EditableNamespaceFinder again.
        return [*paths, PATH_PLACEHOLDER]

    @classmethod
    def find_spec(cls, fullname: str, target=None) -> ModuleSpec | None:  # type: ignore
        if fullname in NAMESPACES:
            spec = ModuleSpec(fullname, None, is_package=True)
            spec.submodule_search_locations = cls._paths(fullname)
            return spec
        return None

    @classmethod
    def find_module(cls, _fullname) -> None:
        return None


def install():
    if not any(finder == _EditableFinder for finder in sys.meta_path):
        sys.meta_path.append(_EditableFinder)

    if not NAMESPACES:
        return

    if not any(hook == _EditableNamespaceFinder._path_hook for hook in sys.path_hooks):
        # PathEntryFinder is needed to create NamespaceSpec without private APIS
        sys.path_hooks.append(_EditableNamespaceFinder._path_hook)
    if PATH_PLACEHOLDER not in sys.path:
        sys.path.append(PATH_PLACEHOLDER)  # Used just to trigger the path hook
